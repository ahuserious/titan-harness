# P8 contract — terraform and simulated users (titan-harness 0.8.0)

Plan: §2 rows H7, H8, H9; §5.7 (connectors/automations), §5.8 (runners); §7 P8 checklist (per command, fail-closed evidence kinds, provider probe matrix, setup-agent path); D15. Machine facts (verified 2026-09-15): Orca CLI (`/home/danbot/Dev Tools/bin/orca`) has NO browser automation (`orca tab` = list|show|current|switch|create --url|profile|close; `orca computer` = accessibility click/type/get-app-state, no screenshot); `orca automations create --name --trigger <hourly|daily|weekdays|weekly|cron|RRULE> --prompt --provider <codex|claude|gemini> [--precheck] [--repo|--workspace]`; `orca terminal split --terminal <handle> --direction horizontal|vertical --command <text>`; `orca linear` reads Linear tickets. Browsers: `/usr/bin/brave-browser` (Chromium, supports `--headless=new --remote-debugging-port`), Playwright's `~/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome` and `chromium_headless_shell-1243/…/headless_shell`, ffmpeg at `~/.cache/ms-playwright/ffmpeg-1011/ffmpeg-linux` (none on PATH); Node 24 has a global `WebSocket`; kane-cli not installed; `uv` at ~/.local/bin/uv. So the sim-user driver is a dependency-free CDP client over a headless Chromium; Kane is preferred when installed; the Orca-tab path is probed and reported unavailable on this build.

Owners:
- "terraform": modules/cmd-terraform.ts, .pi/titan-harness/workflows/terraform/{terraform.yaml,commands/*.md}, prompts/SYSTEM_PROMPT_TERRAFORM.md, prompts/USER_PROMPT_TERRAFORM_{ENTITY,ONTOLOGY,ROADMAP,AUTOMATIONS,CONNECTORS}.md, modules/connectors.ts, skills/titan-terraform/SKILL.md, tests/{terraform,connectors}.test.ts.
- "sim-users": modules/cdp-browser.ts + scripts/cdp-browser.mjs, scripts/stitch-video.mjs, modules/workflow/runners/cdp-browser.ts (+ register it in runners/index.ts and add "cdp" to EvidenceSource in evidence.ts — the two edits allowed outside your files), modules/cmd-local-dev-verify.ts, prompts/USER_PROMPT_SIM_USER.md, skills/titan-local-dev-verify/SKILL.md, tests/{cdp-browser,local-dev-verify}.test.ts + tests/fixtures/site/ (a static fixture site: index.html with a form, a link, a console.log and a fetch).
- "cloud-sim": modules/cmd-cloud-sim.ts, modules/cloud-providers.ts, .pi/titan-harness/templates/cursor.yaml, .pi/titan-harness/templates/remote-testing.md, skills/titan-cloud-simulated-users/SKILL.md, tests/cloud-sim.test.ts.
Host wiring (commands registered through deps seams, level-2 `harness_defaults` consumption in levels.ts, `/terraform` notify at level 3) is the lead's.

## /terraform (owner terraform)
`registerTerraformCommand(pi, deps: { cwd(ctx); store(); notify; panel; runWorkflow(name, inputs, ctx): Promise<RunResult> /* the host's workflow runner */; ultraplanTeam(ctx): { seats: ResolvedRole[]; judge?: ResolvedRole; fuser?: ResolvedRole } ; mcpBridge?(): { mcpTool } ; confirm(ctx, title, body): Promise<boolean> })` → `/terraform [--refresh] [--section entity|ontology|roadmap|automations|connectors] [--dry-run]`:
- Runs the shipped package workflow `terraform` (a YAML DAG the engine executes; nodes: `gather` (bash: list repo docs, package.json/pyproject, README, AGENTS.md, vision.md, intent.md, .titan/*.md, git remotes; writes `sources.json` with sha256 per file to $ARTIFACTS_DIR), `entity` (role fusion ×3 via best_of n=3 → judge; prompt USER_PROMPT_TERRAFORM_ENTITY.md: domain, organization, audience, platform, infrastructure, talent, financials, intent, vision, bias — each section cites sources by digest; unknowns marked `unknown`, never invented), `ontology` (mcp_tool infranodus generate_ontology_graph over entity.md when the bridge is keyed; `when` guard on a probe node output; else a prompt node producing a `declared`-confidence ontology with a banner line), `roadmap`, `automations` (which triggers may call /create-workflow; opt-in list), `connectors` (reads/creates `.titan/terraform/connectors.yaml`: MCP servers from the catalog (macro, figma, brandfetch, testmu, infranodus), `gh` CLI, `orca linear`, script nodes with env-named credentials), `verify` (role verifier: research-planning tier — alignment table of every claim vs sources; execution claims forbidden), returns `report`).
- Host-side persistence: the command copies the run's artifacts into `.titan/terraform/{entity,ontology,roadmap,automations,connectors}.md`, each ending with a "Sources" table (path, sha256) and a `harness_defaults:` YAML block in entity.md ({ level: 2, tier, review, exa, budget_usd, personas }) that levels.ts consumes at level 2 (the lead wires `readHarnessDefaults(cwd)` — export it from modules/connectors.ts? no: from cmd-terraform.ts as a pure function `readHarnessDefaults(cwd): HarnessDefaults | undefined`). `--dry-run` prints the DAG plan and the source list. Refuses to overwrite without `--refresh` (or per `--section`).
- connectors.ts: `loadConnectors(cwd)`, `dryRunConnectors(cwd, probes: { mcpServers(): string[] /* enabled catalog names */; which(bin): string | undefined; env(name): boolean })` → per connector `{ name, kind: mcp|cli|script|orca, reachable: boolean, reason }` (never prints values), rendered as the probe table; `automations.md` schedule lines are emitted as ready-to-run `orca automations create --name titan-<wf> --trigger <preset|cron> --prompt "pi -p \"/workflow run <wf>\"" --provider claude --workspace current` recipes (printed, not executed).
Tests: the terraform workflow validates (validateFile) and runs end-to-end with the stub harness (fake agent answers per node, fake bash) producing the five docs with Sources tables and the harness_defaults block; the InfraNodus-absent path yields a `declared` banner; readHarnessDefaults parses the block; connectors dry-run marks vacant vs reachable from fake probes; overwrite refusal without --refresh.

## /local-dev-verify (owner sim-users)
cdp-browser.ts (pure Node): `findChromium(): { path: string; kind: "headless-shell" | "chrome" | "brave" } | undefined` (order: TITAN_CHROMIUM env, ~/.cache/ms-playwright/chromium_headless_shell-*/chrome-linux/headless_shell, ~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome, brave-browser/google-chrome/chromium on PATH); `findFfmpeg()` (PATH, then ~/.cache/ms-playwright/ffmpeg-*/ffmpeg-linux); `class CdpBrowser { static launch(opts: { chromium?: string; userDataDir: string; width?: number; height?: number; timeoutMs?: number }): Promise<CdpBrowser> /* spawns `<chromium> --headless=new --remote-debugging-port=0 --user-data-dir=<dir> --no-first-run --no-default-browser-check --disable-gpu about:blank`, reads the ws URL from stderr "DevTools listening on ws://…" or <userDataDir>/DevToolsActivePort */; page(): Promise<CdpPage>; close(): Promise<void> }`; `class CdpPage { goto(url, { waitUntil?: "load" | "networkidle"; timeoutMs? }); snapshot(maxChars?): Promise<string> /* document.body.innerText + a11y-ish list of links/buttons/inputs via Runtime.evaluate */; click(selectorOrText); fill(selector, value); press(key); eval(expression): Promise<unknown>; screenshot(file): Promise<{ path; bytes }> /* Page.captureScreenshot png */; consoleLog(): Array<{ level; text; ts }>; networkLog(): Array<{ url; method; status?; type?; ts }>; waitFor(selectorOrText, timeoutMs) }` — JSON-RPC over the global WebSocket with ids, event subscriptions (Runtime.enable, Network.enable, Page.enable, Log.enable); every method has a timeout; errors carry the CDP message. scripts/cdp-browser.mjs: a CLI over the same logic (plain JS, mirrors the TS; a test compares behaviour on the fixture site) — `node scripts/cdp-browser.mjs run --flow <flow.json> --out <dir> [--chromium <path>]` executes a flow `{ url, steps: [{ goto | click | fill | press | wait | screenshot | snapshot | eval }] }` and writes `step-<n>.png`, `snapshot-<n>.txt`, `console.json`, `network.json`, `flow-result.json` {ok, steps: [{n, ok, error?}]}. scripts/stitch-video.mjs: `--frames <dir> --out flow.mp4 [--fps 2]` via findFfmpeg (exit 3 "ffmpeg unavailable" when absent).
runners/cdp-browser.ts: runner name "cdp-browser" — spec `{ runner: cdp-browser, flows: [{ name, url, steps }] | flows_file, video?: boolean }`; findChromium absent → unavailable "no Chromium (Playwright cache, brave-browser or chrome) on this machine"; runs each flow through the CLI (ctx.exec node scripts/cdp-browser.mjs) into evidenceDir/<flow>/, hashes screenshot/snapshot/console-log/network-log kinds (source "cdp", observed), `video` kind when stitch succeeds else `missingInformation: ["video: unavailable"]`; pass iff every flow-result ok.
cmd-local-dev-verify.ts: `registerLocalDevVerifyCommand(pi, deps: { cwd; store; notify; panel; runtime(ctx, loaded, runId, runDir) /* the host's workflow runtime factory */; confirm; which })` → `/local-dev-verify [--url <u>] [--start "<cmd>"] [--flows <file.json>] [--workers <n>] [--no-video]`: (1) app start: `--start` or a recipe by project type (package.json scripts dev|start → `npm run dev`; pyproject → `uv run <main>`; Procfile web:), spawned in a process group with logs to the run dir, then probe `--url` (default http://localhost:3000, or the port the start log prints) with fetch until 200/3xx or timeout 60 s → else unavailable; (2) driver choice: kane-cli on PATH → runner kane; else findChromium → cdp-browser; else → panel "unavailable: no Kane, no Chromium" and exit (never a silent pass); (3) sim-user workers: `--workers` (default settings.workerFanOut, cap 5) fresh-context worker prompts (USER_PROMPT_SIM_USER.md: persona sim-user, the URL, a page snapshot from the driver, ask for 3–5 realistic flows as JSON steps) → flows executed by the runner; (4) evidence packages per flow + an architect inbox row (event `inbox.architect` {summary, evidencePaths}) + the final panel (flows table: name, steps ok/total, screenshots, video yes/unavailable, evidence status); the app is killed at the end (process group). Implemented as an in-memory workflow doc executed by the engine (nodes: probe (bash), snapshot (bash cdp CLI), sim-users (best_of-less: N worker prompt nodes with output_format flows schema), verify (cdp-browser runner), report) so everything lands in the store.
Tests (no real model; real headless Chromium when findChromium succeeds, else the driver tests skip with a message): serve tests/fixtures/site via a tiny node http server on an ephemeral port; CdpPage goto/snapshot/click/fill/screenshot/console/network against it; the CLI writes the artifacts and flow-result.json; the runner produces a matched evidence package with the four kinds (+ video unavailable when ffmpeg is absent — assert the missingInformation line rather than requiring ffmpeg); stitch-video exits 3 without ffmpeg; recipe detection table; probe timeout → unavailable; the command's workflow doc validates.

## /cloud-simulated-users (owner cloud-sim)
cloud-providers.ts: registry `[cursor-cloud, testmu-hyperexecute, kane-remote, momentic]` with `probe(deps: { env(name): boolean; which(bin); mcpEnabled(server): boolean; fetch? })` → `{ provider, ready: boolean, reason, needs: string[] /* env names, CLIs */, skill: string }`; cmd-cloud-sim.ts: `registerCloudSimCommand(pi, deps: { cwd; store; notify; panel; runtime; confirm; which; env; mcpEnabled; runChild; workerSeat() })` → `/cloud-simulated-users [probe|setup <provider>|run <provider> --objective "<text>" [--devices a,b]|advice]`: `probe` (default) prints the matrix (ready/vacant with the missing names, never values); `setup <provider>` spawns a worker child with that provider's skill (testmu-cloud-testing / kane-cli-browser-runs / momentic-e2e / a cursor-cloud reference in the skill) that writes `.titan/terraform/remote-testing.md` advice (host persists the child's structured output; the child is read-only; no global installs without `deps.confirm`); `run <provider>` executes the matching P4 runner through a one-node verify workflow and streams `cloud-sim.*` events (≥ 3 for cursor: preflight, started, polled…, finished) into the store, panel with the evidence hash; `advice` prints remote-testing.md. Templates: `.pi/titan-harness/templates/cursor.yaml` (`env: { type, name }`, `repo`, `startingRef: titan/<runId>`) and `remote-testing.md` skeleton. Tests: matrix from fake probes (all vacant today; one ready with fake env); setup path persists the advice from a fake child result and never writes outside .titan/terraform; run path with the recorded-cursor fake fetch (reuse the P4 fixtures) emits ≥ 3 events and a hashed evidence artifact; a vacant provider refuses `run` with the missing names.

All builders: whole suite green, no commits, report files + API deviations + checklist coverage + suite summary. Skills index (skills/README.md) entries and the docs are the docs builder's, not yours.
