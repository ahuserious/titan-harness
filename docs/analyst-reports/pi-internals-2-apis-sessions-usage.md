# Pi internals analyst — condensed facts (Parts 1-6a)
PI=/snap/pi-coding-agent/current/lib/pi/packages/coding-agent; DW=~/.pi/agent/npm/node_modules/@quintinshaw/pi-dynamic-workflows; SA=~/.pi/agent/npm/node_modules/pi-subagents; TH=~/Dev Tools/pi-extensions/titan-harness

## A2 plan mode: Pi has NO built-in plan mode (docs/usage.md:309). Reference implementation to copy: PI/examples/extensions/plan-mode/ (index.ts 12KB: registerFlag("plan"), /plan + /todos, Ctrl+Alt+P, setStatus+setWidget tracker, bash allowlist via tool_call block, tool swapping via setActiveTools with PLAN_MODE_DISABLED_TOOLS={edit,write}, [DONE:n] marker parsing on message_end, state survives resume).
## A3 shift+tab: RESERVED. runner.js:7-25 RESERVED_KEYBINDINGS_FOR_EXTENSION_CONFLICTS includes app.thinking.cycle (shift+tab); runner.js:376-380 skips the extension shortcut with a diagnostic. User frees it in ~/.pi/agent/keybindings.json (flat JSON action→key): {"app.thinking.cycle": "alt+t"} or {"app.thinking.cycle": []} then /reload. After that an extension may bind shift+tab. Non-reserved built-ins are overridable with a warning.
## A4 UI: dialogs select/confirm/input/editor (timeout, signal); notify/setStatus/setTitle/setEditorText; setWidget(key, lines|component, {placement:"aboveEditor"|"belowEditor"}); setFooter; setWorkingMessage; addAutocompleteProvider; setEditorComponent; themes. NO sidebar/split/tabs/terminal-pane API. Persistent unfocused overlay: ctx.ui.custom(factory, {overlay:true, overlayOptions:{anchor:"right-center", width:"40%", minWidth:40, maxHeight:"80%", visible:(w,h)=>w>=100}, onHandle:h=>h.unfocus({target: editorComponent})}) (extensions.md:2757-2793; tui.md:124-197): handle.unfocus({target}) keeps overlay visible while the editor gets input; handle.setHidden/hide/focus. Experimental; overlay components disposed on close (re-call custom to re-show); mouse routing only under --tui-mode fullscreen; ctx.ui.custom is TUI-only (guard ctx.mode==="tui"). Verdict: /workflow-monitor can be a belowEditor widget (cheapest) or an unfocused right-anchored overlay (richest in-process); external pane only via Orca/tmux.
## A5 sessionManager (read-only): getEntries/getBranch/getTree/getEntry/getLeafId/buildContextEntries/buildSessionContext/getSessionId/getSessionFile/getSessionDir/isPersisted. Compaction entry {type:"compaction", summary, tokensBefore, retainedTail: AgentMessage[]} — retainedTail makes compactions self-contained checkpoints; firstKeptEntryId legacy. ctx helpers: getContextUsage()→{tokens,contextWindow}; compact({customInstructions,onComplete,onError}); getSystemPrompt(); isIdle/abort/hasPendingMessages; command ctx adds waitForIdle/newSession/fork/switchSession/reload. Usage on every AssistantMessage: {input, output, cacheRead, cacheWrite, totalTokens, cost:{input,output,cacheRead,cacheWrite,total}}; at message_end event.message.usage is mutable; ToolResultMessage.usage carries nested LLM work; core aggregator dist/core/usage-totals.js getUsageCostBreakdown(entries). pi.events = in-process cross-extension bus (extensions.md:1729). input hook: extension /commands checked first and bypass input; input sees raw text before skill/template expansion.

## B pi-dynamic-workflows 3.10.1 — CORRECTION: workflows are JAVASCRIPT scripts (saved as <name>.json envelopes {name, description, script, parameters, savedAt, path, source, location}); NO YAML schema; NO /create-workflow command. Authoring = run then `/workflows save <name> [runId]` or hand-write a script with the workflow-authoring skill. /workflows subcommands: run <prompt> | status/watch <id> | stop | pause | resume | rm | save | list | ui; bare /workflows opens navigator (titan patch hooks it). Script contract: `export const meta = {name, description, phases:[{title, detail?, model?}], model?}`; agent(prompt, {label, phase, schema (typebox → structured output via terminating tool), model ("provider/id" with optional :thinking suffix), tier small|medium|big, isolation:"worktree", thread, agentType (.pi/agents/<name>.md), timeoutMs, retries}); precedence explicit model > agentType model > tier > phase model > session model. Helpers: parallel, pipeline, workflow(saved), verify, judgePanel, loopUntilDry, completenessCheck, retry, gate, checkpoint({default, headless:"default"|"abort", kind:confirm|input|select} — journaled human gates), phase, log, budget. Agents run IN-PROCESS via createAgentSession (not subprocesses); orchestration tools always denied; excludeSubagentTools adds denials. Runtime events type: phase | workflow start/end | quality (verify/judgePanel/completenessCheck) | control-attempt (retry/gate). Persistence: ~/.pi/workflows/projects/<basename>-<sha256(abspath)[0:12]>/{runs,saved,settings.json,model-tiers.json}; caps MAX_CONCURRENCY 16, MAX_AGENT_RETRIES 3. Machine state: only ~/.pi/workflows/settings.json {"defaultConcurrency":4}; no saved workflows (greenfield). Commands: workflows, effort, ultracode, deep-research, adversarial-review, code-review, multi-perspective, codebase-audit, workflows-models, plus one per saved workflow. Navigator: workflow-ui.ts openWorkflowNavigator → ui.custom (NOT overlay; takes over editor); twoPaneHeader() (:1551-1582) renders 2 header lines (status · done/total agents · tokens) — natural home for totals; per-row meta done/total · tokens · cost; task-panel.ts installTaskPanel = below-editor live panel (compact|detailed). WorkflowManager extends EventEmitter: started, log, phase, agentStart, agentModel, agentUsage, tokenUsage, agentEnd, agentHistory, complete, paused, error, stopped, resumed — BUT never published on globalThis (only a 30s reload-handoff symbol). Options: poll run JSON, patch dist (as titan does), or upstream a Symbol.for("pi-dynamic-workflows:manager") hook. Persisted run state: {runId, workflowName, script, args, sessionId, status pending|running|paused|completed|failed|aborted, error, pauseReason, resetHint, phases[], currentPhase, agents[{id, callId, label, phase, prompt, status queued|running|done|error|skipped, result, tokens, tokenUsage, model, startedAt, endedAt}], logs[], result, startedAt, updatedAt, completedAt, durationMs, tokenUsage{input,output,total,cost,cacheRead,cacheWrite}, journal[], autoResume}. Settings (~/.pi/workflows/settings.json): keywordTriggerEnabled, keywordTriggerWord, defaultAgentTimeoutMs, defaultTokenBudget, defaultConcurrency (≤16), defaultAgentRetries (≤3), progressPanelMode, progressPanelMaxAgents, persistAgentSessions, deliveredResultMaxChars, excludeSubagentTools[]. Per-workflow model/thinking: meta.model + meta.phases[].model with :thinking suffix (e.g. gpt-5.5:xhigh); tiers in model-tiers.json via /workflows-models. Titan patch confirmed at dist/workflow-commands.js:175-190 (destroyed by pi update --extensions or bump; .orig restore).

## C pi-subagents 0.67: tool name is `subagent` (not delegate); titan SUBAGENT_TOOL="subagent". Background spawn: Pi-binary host argv ["--no-extensions","--no-skills","--no-prompt-templates","--no-session","--mode","rpc","--extension",bootstrap]; bootstrap re-registers needed pieces. Foreground children (async:false) NEVER load ambient extensions → antigravity/* and cerebras/* provider models need background children. Tool params: agent, task, action, topic, config, context fresh|fork|profile, model (":thinking" suffix overrides thinking), missionId/mission, handoffPath, repo, planId, mode, laneId, merge, focus, agentScope, capabilities, async, chatProgress, isolation none|worktree, baseRef, timeoutMs/maxRuntimeMs, toolTimeoutMs, toolBudget{soft,hard,block}, usageBudget{tokens,costUsd}, cwd, maxOutput, artifacts, includeProgress, share, sessionDir, acceptance, gate. No per-call tools param (tools from agent definition/profile + settings modelScope + toolBudget.block). Config ~/.pi/agent/extensions/subagent/config.json {globalConcurrencyLimit:4} (+ modelResponseAliases, modelExclusions, toolDescriptionMode, timeoutMs); settings-level keys in Pi settings.json subagents.* (defaultModel, defaultProvider, defaultThinking, defaultExtensions, agentOverrides, agentScanDirs, modelScope, disableThinking, disableBuiltins, projectRootResolution, watchdog). Events consumable in-process: subagent:async-started {task, goal, workflowGraph, asyncDir}, subagent:async-complete, subagent:control-intercom, subagent:result-intercom; pi.events is in-process only — cross-process via file lifecycle artifacts or pi-intercom. council-mode: 2–4 read-only council-* advisors, pass 1 independent → pass 2 cross-exam → optional pass 3, ONE async workflowScript with runs.all, parent memo.

## D titan-harness 0.2.0 modules (TH/extensions/titan-harness/modules): runtime.ts 458 (roles/glyphs/colors, AgentRun→AgentStat, READONLY_TOOLS/FULL_TOOLS, HarnessDeps seam; no pi APIs), model-stack.ts 273 (shape YAML loader; validators), stack-config.ts 196 (settings store, 3 Symbol.for hooks, childToolsFor, subagentCapHint, modelFamily, isStackChild), child-runner.ts 357 (piInvocation + runChild spawn `pi --mode json -p` streaming JSON events into AgentRun; runProc; process groups SIGTERM→SIGKILL), prompt-library.ts 349 (prompts/*.md; strict parsers), tui.ts 130 (model-bar text only), audit.ts 158, cmd-readonly.ts 165 (/titan-opinion, /titan-debate), cmd-fusion.ts 177 (/titan-fusion sole-writer FUSION + SHA-256 ACK), cmd-build.ts 795 (/titan-collaborate DAG exec; /titan-auto-validate), collaboration-graph.ts 95 (CollaborationTask {id, assignee, description, depends_on[], outputs[], mode read|write}), writer-lease.ts 64. Top-level titan-harness.ts 1554, stack-settings.ts 513 (/stack), ctx-picker.ts 270 (/ctx). Tests via bun test: model-stack, collaboration-graph, orchestration-contract, writer-lease.
Shape YAML = bare list of slots: name /^[A-Za-z0-9_-]{1,16}$/, model provider/id, thinking aliases (default medium), color #hex (else palette), architect:true on exactly one, primary:true on exactly one (live host builder), system_prompt (inline or path; missing path = hard error), append_system_prompt (string|list; via repeatable --append-system-prompt). Codename = filename minus model-stack-; STACK_DIR ~/.pi/titan-harness. ModelStack {codename, configPath, slots, architect, primaryBuilder, builders}.
DEFAULT_STACK_SETTINGS: subagentTools true, childSubagents "all", childExa true, subagentModel cerebras/qwen-3.8-27b, subagentThinking high, subagentFanOut 4, shape astra-gemini (live: consult), builderFanOut 2, auditor true, auditorModel auto, auditorThinking high, auditRounds 2, anonymize true, modelBar true. Ranges subagentFanOut 0-16, builderFanOut 1-4, auditRounds 1-3. readStackSettings never rewrites on parse failure; writeStackSettings atomic temp+rename 0600. Hooks Symbol.for("titan-harness:model-bar"|"workflows-menu"|"shape-changed"). childToolsFor adds `subagent` when subagentFanOut>0 and (childSubagents all | builders+can-write), adds 4 EXA tools when childExa, "none" when !subagentTools. subagentCapHint: tier-3 contract appended to every child prompt.
