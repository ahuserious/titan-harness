# Model / provider facts verified on this machine (2026-09-14)
Pi auth.json credentials present: openai-codex (oauth, INVALIDATED server-side → /login openai-codex needed), xai (oauth), antigravity (oauth, Gmail), cerebras (api_key), exa (api_key). No OpenRouter key (auth.json or env). No XAI_API_KEY / OPENROUTER_API_KEY / INFRANODUS_API_KEY in env (InfraNodus key lives in Claude Code's ~/.claude.json env for the stdio server; not printed).
Thinking-level support (Pi provider data /snap/pi-coding-agent/current/lib/pi/packages/ai/dist/providers/data/*.json and package sources):
- xai/grok-4.6: low|medium|high|xhigh (ctx 500k; $2/$6 per M) — xhigh OK. grok-4.5: max high.
- openai-codex/gpt-6-astra: low..xhigh|max (Pi builtin ctx 272k; user override 872k in ~/.pi/agent/models.json; tiered pricing above 272k input). Requires Codex OAuth re-login.
- openrouter/meta/muse-spark-1.3: minimal..xhigh|max, ctx 1,048,576, $1.25/$4.25 per M — ONLY via OpenRouter (needs OPENROUTER_API_KEY). muse-spark-1.3-contributor variant $0.10/$0.20.
- gemini-3.8-flash: OpenRouter map max high (xhigh null); antigravity maps xhigh → "gemini-3.8-flash-high" (effective high); google provider entry has no thinking map. So "xhigh" for Gemini 3.8 Flash = effective high everywhere.
- cerebras/qwen-3.8-27b (custom entry in models.json): off|low|medium|high; xhigh/max null; ctx 131072; maxTokens 40960; $0.99/$1.49 per M. "xhigh" = effective high. Cerebras built-in Pi list has gemma-4-31b, gpt-oss-120b (no qwen) — the custom entry is required.
- anthropic/claude-fable-5-1: Pi favorite; thinking xhigh/max supported (github-copilot entry shows xhigh/max map; anthropic direct similar).
- Grok CLI (grok.com subscription login): grok-4.6 (default) and grok-4.5; headless `grok -p ... --output-format json`; `--permission-mode plan` cancels the run on the first shell Execute (needs approval) → use `--permission-mode dontAsk` or `--tools` allowlist for headless analysts; `--resume <sessionId>` continues with all prior reads; ~$0.008 per trivial call, $0.19 for a 10-turn repo read.
- Fusion Drive (claude-fusion-drive 0.2.2): active profile xai-claude-oauth NOT ready (XAI_API_KEY absent). subscription-oauth profile is ready (claude_oauth + grok_oauth): panel grok45-oauth-panel, grok45-oauth-panel-b, fable5-oauth-panel; judge grok45-oauth-judge; fuser fable5-oauth-fuser; gates grok45-oauth-gate-primary/secondary; Grok 4.5 xhigh → effective high (provider_ceiling); Claude xhigh → max (provider_equivalent). Host goal tool claude_code.TaskCreate not exposed in this host (affects execution phase only).
- InfraNodus MCP: npm infranodus-mcp-server 1.8.4 (bin infranodus-mcp-server; main dist/index.js; local clone /home/danbot/Dev Tools/mcp/mcp-server-infranodus; stdio via `npx -y infranodus-mcp-server` with env INFRANODUS_API_KEY; 14-day trial keys at infranodus.com/api-access). Already registered in Claude Code (~/.claude.json) and Codex; NOT in ~/.config/mcp/mcp.json (Pi) yet.
- Orca CLI: `orca terminal create|split|send|read|wait|rename|close`, `orca worktree create`, `orca orchestration run-create|dispatch|gate-create|gate-resolve|send|inbox`, browser automation (tab create, goto, snapshot, click, fill, screenshot, eval), iOS emulator commands, `orca automations create|run`.
- VERIFIED 2026-09-14 22:45: `pi --list-models` exposes ONLY antigravity (14 models incl. claude-opus-4-6 250K, claude-sonnet-4-6 200K, gemini-3.8-flash…), openai-codex (8; OAuth invalid), xai (3 incl. grok-4.6), cerebras (3). There is NO anthropic provider and NO openrouter provider configured: `anthropic/claude-fable-5-1`, `anthropic/claude-opus-5`, `openrouter/openai/gpt-6-astra` all return "No models match". Consequence: every Fable 5.1 slot in the brief (level-3 builders, /ultraplan fusion seat and fuser, fallbacks) is NOT runnable from titan children until an Anthropic API key (or Pi Anthropic login) or an OpenRouter key is added. Closest available Claude today: antigravity/claude-opus-4-6.
