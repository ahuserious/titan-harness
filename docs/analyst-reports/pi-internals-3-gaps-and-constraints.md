# Pi internals analyst — Parts 6b/7/8 (condensed)
## D4 Model bar (titan-harness.ts:744-850): FOOTER_WIDGET belowEditor via ctx.ui.setWidget(key, component, {placement:"belowEditor"}); rows: one per slot (cellStr: ◆ ROLE | name | model (thk) | [bar] % | tps | $; context bar uses modelRegistry contextWindow with 1,000,000 fallback; idle primary slot shows host model + ctx.getContextUsage()), FAN-OUT (fanOutSnapshot), SUBAGENT, EXA, SHAPE, one AUDITOR row per builder (auditorState via noteAuditor callback). 1 Hz setInterval(renderFooterWidget).unref(); whole render in try/catch ("progressive enhancement — never break the session").
## D5 Child argv (child-runner.ts:62-94): --mode json -p --session-dir <dir> --no-skills --no-context-files --thinking <lvl> --model <provider/id> [--fork <sessionFile> | --session <id> | --session-id <uuid>] [--system-prompt <text>] [--append-system-prompt <text>]… [--append-system-prompt <subagentCapHint>] (--no-tools | --tools <csv>) <prompt>. piInvocation() reuses the running pi entry. Env TITAN_HARNESS_CHILD=1; children DO load host extensions (antigravity/*, pi-subagents, pi-exa resolve) — titan extensions return early in children. Sessions /tmp/titan-harness-sessions/<projectSlug>/run-<pid>/<slotId>, keyed per slot AND model (replaying one model's transcript into another tripped Anthropic's usage classifier); rm -rf on session_shutdown.
## D6 Audit gate (audit.ts): PASS/PASS_WITH_WARNINGS forwarded with verdict; FAIL → bounded correction rounds (settings.auditRounds) then AUDIT_EXHAUSTED (architect arbitrates); SAFETY/SCOPE_VIOLATION fail closed; INCONCLUSIVE/AUDIT_ERROR forwarded with note. Auditor = read-only child, fresh session per review, cross-family (modelFamily mismatch). Inputs: task + acceptance + builder report + scoped `git diff HEAD` (DIFF_MAX 60,000 chars) + --stat + untracked. hasWorkingTreeChanges() skips chat-only answers. Corrections resume the builder's own session via run.sessionRef. Callsigns only both directions.
## D7 Hotkeys: bindKey(keys[], description, handler) → pi.registerShortcut per key (try/catch). ctrl+tab/alt+h cycleShape; ctrl+shift+n/alt+n BUILDER_FANOUT_CYCLE [1,2,3,4]; ctrl+shift+s/alt+s SUBAGENT_FANOUT_CYCLE [0,2,4,6,8]; ctrl+shift+a/alt+a auditor toggle. cycleShape walks listShapes(), loads the first shape with zero problems (skips unauthed slots), warns if none. Setters write settings then announce() (notify + re-render).
## D8 tps/cost: turn_start → hostTurnStart; before_provider_request → hostRequestStart; message_end (assistant) → bumpSlotPerf(primaryBuilder.id, usage.output, seconds since request start, usage.cost.total). message_start is NOT a safe first-token clock (10k+ TPS artifacts). slotPerf Map<slotId,{outputTokens, seconds, costUsd}>; child runs absorbed once (absorbedRuns set). Child runs (child-runner.ts:162-183) consume input+cacheRead+cacheWrite (prompt), output, cost.total, totalTokens (ctx); child tps excludes tool time (segment re-opens at tool_execution_end). runTps = tokensOut/tpsSeconds.
## D9 Watchdog/resume-after-compaction: NOTHING exists in titan (no session_before_compact/session_compact handlers, no stall detector). Only child session resume via AgentRun.sessionRef / SpawnIdentity {fork?, sessionDir, sessionId?, resume?} used by audit corrections, auto-validate rounds, fusion retries. Greenfield.
## D10 Skills: package.json pi.skills ["./skills"]; 16 skills; private:true, type:module, dep yaml; bundling rule (packages.md:171): @earendil-works/pi-ai, pi-agent-core, pi-coding-agent, pi-tui, typebox must be peerDependencies "*", never bundled.
## E1 pi-exa 0.6.1 tools: deep_search_exa (direct; needs Exa key), web_search_exa, web_fetch_exa, web_search_advanced_exa (MCP-proxied). Titan EXA_TOOL_NAMES hardcodes all four.
## E2 pi-mcp-adapter 2.33.0 package-local MCP: YES. Manifest `"pi": { "mcp": "./mcp/mcp.json" }` (string or array of package-relative paths); adapter reads packages[] from project .pi/settings.json then ~/.pi/agent/settings.json; relative path packages resolve (titan is "../../Dev Tools/pi-extensions/titan-harness"); config must stay inside the package; only mcpServers entries load; server names namespaced <sanitized_package>__<server> (titan_harness__infranodus); user/global/project MCP config outranks package config; duplicates skipped. Config layers lowest→highest: ~/.config/mcp/mcp.json · ~/.agents/mcp.json · ~/.agents/mcp/mcp.json · ~/.pi/agent/mcp.json · project .mcp.json · project .pi/mcp.json (where /mcp disable|enable persists). Host configs (~/.claude/mcp.json etc.) detected but not loaded unless settings.hostConfigDiscovery "on". Server fields: url | command+args+env, auth oauth|bearer, protocolVersion, directTools, disabled, toolPrefix server|short|none|mcp, bearerTokenStore, oauth{clientId}. Runtime registration without import: emit "pi-mcp-adapter:runtime-register:v1" on pi.events; MCP_STATUS_EVENT snapshot; pi-mcp-adapter/oauth token helpers.
## E3 Titan catalog is inert today: TH/package.json has NO pi.mcp key; install is manual (scripts/install-mcp.mjs). Adding "mcp": "./mcp/mcp.json" loads the catalog automatically as titan_harness__* — the mechanism to bundle infranodus.
## Constraints & gotchas (14): (1) shift+tab reserved (app.thinking.cycle); rebind via ~/.pi/agent/keybindings.json then /reload; (2) no plan mode/sidebar/split/terminal-pane API — monitor = belowEditor widget or experimental overlay with handle.unfocus({target: editor}); (3) titan already owns belowEditor (6+ rows) — extend renderFooterWidget rather than add a widget; (4) before_provider_headers not re-fired on retry; before_provider_request payload rewrites invisible to getSystemPrompt(); (5) message_start not a first-token clock — reuse before_provider_request→message_end + bumpSlotPerf; (6) session_before_compact can cancel or replace the summary; willRetry distinguishes overflow recovery; build resume on compaction entry retainedTail; (7) sessionManager read-only; extensions write via pi.appendEntry/pi.sendMessage/pi.setLabel; (8) dynamic-workflows = sandboxed JS (no imports/fs/Date.now/Math.random); (9) titan /workflows patch in node_modules dist is wiped by updates — prefer upstream hook; (10) no live WorkflowManager handle — poll ~/.pi/workflows/projects/<key>/runs/*.json or upstream a registry symbol; (11) pi.events in-process only — titan children are separate processes; JSON event stream is the only channel; (12) pi-subagents foreground children load no ambient extensions; no per-call tools allowlist; thinking via provider/id:high suffix; (13) one fan-out knob writes three files (titan-harness.json, subagent config.json globalConcurrencyLimit, workflows settings.json defaultConcurrency ≤16); last-write-wins; (14) titan MCP catalog inert until pi.mcp is declared. Corrections to the brief: dynamic-workflows has no YAML schema and no /create-workflow; titan ships 16 skills.
