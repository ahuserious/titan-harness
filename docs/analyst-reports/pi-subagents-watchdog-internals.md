# pi-subagents 0.67 watchdog — implementation map (analyst: watchdog-internals)
Root: ~/.pi/agent/npm/node_modules/pi-subagents/ ; K-Dense does not vendor it, it configures it (server/src/agent/watchdog-settings.ts:28-53 writes enabled, main.model, main.thinking, cadence.everyNTools, severityThreshold, children.enabled, guidance.watchdogMd, stalemateRepeats; forces lsp.enabled:false; seeds sandbox/.pi/WATCHDOG.md).

Files: src/watchdog/register-main.ts:380-445 (entry, /subagents-watchdog command, event subscriptions); runtime.ts:126-913 (state machine: delta buffering, trigger gating, timeouts, stalemate, LSP pre-pass); review.ts:253-379 (nested Agent with read-only tools; fallback chain), review.ts:221-247 (prompt template); settings.ts:62-132,487-518 (strict schema; user→project→session merge; writers writeUserWatchdogEnabled :464, writeWatchdogModelSettings :474); types.ts (SUBAGENT_WATCHDOG_WARNING_TYPE="subagent_watchdog_warning"); rules.ts (role→model allow/deny globs); guidance.ts (WATCHDOG.md loader: <cwd>/.pi/WATCHDOG.md then ~/.pi/agent/WATCHDOG.md, 8k cap); scope.ts; diff-tool.ts (watchdog_diff: git diff since session-start HEAD + untracked, 24k cap); lsp-diagnostics.ts; emission-guard.ts (dedupe/identity); turn-delta.ts (edit/write bodies redacted); change-signature.ts; register-child.ts:51-121 (child-side watchdog); permission-arbiter.ts; tool-actions.ts:112-156 (subagent({action:"watchdog.*"})).
Wiring: src/extension/index.ts:502 registerMainWatchdog(pi); :624 passed to executor; gated by PI_SUBAGENT_CHILD=1 (index.ts:4-9).

Config: namespace subagents.watchdog in Pi settings.json (user ~/.pi/agent/settings.json; project <project>/.pi/settings.json; session in-memory). Strict parse: unknown keys disable the watchdog. Defaults: enabled:false, clarification:false, agentEndTimeoutMs:30000, severityThreshold:"concern", maxWarnings:null, guidance.watchdogMd:true, stalemateRepeats:3, scope.enabled:true, cadence.everyNTools:null (min 5), main:{enabled:false, model?, fallbackModels?, thinking?}, children:{enabled:false, watchdogTailTimeoutMs:120000, model?, fallbackModels?, thinking?, cadence?, overrides:{<agent>:{...}}}, lsp:{enabled:true,timeoutMs:3000,maxFiles:20,maxDiagnostics:50}, rules:{action:"warn"|"block", roleModels:{<agent>:{allow,deny,note}}}. NOTE ~/.pi/agent/extensions/subagent/config.json holds only global child tool permission rules, not watchdog config.

Triggers (register-main.ts:413-442): session_start (git baseline), before_agent_start (reset, scope, re-baseline), turn_end (buffer delta), tool_result (cadence), agent_end (boundary review), input (cancels in-flight review), model_select, session_before_switch/fork, session_compact (hard reset), session_shutdown. Boundary review runtime.ts:389-463 gated on repo change signature differing from turn start and last reviewed; LSP pre-pass; one model review with agentEndTimeoutMs. Cadence review :375-387. Reviewer input bounded to 24,000 chars (scope + changed paths ≤200 + LSP + activity tail + deltas) plus WATCHDOG.md and on-demand read/grep/find/ls/watchdog_diff.

Delivery: only via watchdog_warn tool (review.ts:194-219) → Pi custom message subagent_watchdog_warning (XML + typed details: severity/category/confidence/source/state/summary/evidence/recommendedAction/identity), rendered card; boundary findings trigger exactly one continuation turn; cadence findings deliverAs:"steer". Stalemate (runtime.ts:699-726): identical consecutive warning identity ≥ stalemateRepeats → delivered with state:"stalemate", triggerTurn:false (turn ends). Child findings reach parent via result envelope watchdog.warnings (last 20), acceptance check watchdog-blocker, completion notices, Fleet wd:<n>.

Programmatic surface: NONE exported (package.json exports 14 entrypoints, no watchdog; no globalThis symbol). Reachable: subagent tool actions watchdog.status|check|configure|recommend-model (no "review now"); slash command /subagents-watchdog [status|on|off|session on/off|model|thinking|check|test concern|blocker <text>]; passive observation of subagent_watchdog_warning in session JSONL/event stream (how K-Dense UI consumes it, server/src/agent/events.ts:219). Cannot be pointed at another session/run or a /tmp/pi-subagents-uid-1000 artifact dir. Child watchdogs exist only inside pi-subagents-launched children (resolveChildWatchdogConfig → ChildRuntimeConfig).

Spawn model: background children run in a detached runner process (runs/background/async-execution.ts:596-611): node --import runner-peer-preload.mjs <jiti cli> <runner> <cfgPath>; Pi-binary host variant uses --no-extensions --no-skills --no-prompt-templates --no-session --mode rpc --extension bootstrap; env PI_SUBAGENT_RUNNER_CONFIG, PI_PACKAGE_DIR, PI_SUBAGENT_CHILD=1. Foreground children never load ambient parent extensions; background children do unless agent sets extensions or ceiling denies. Tool allowlists: frontmatter tools (strict; omitted=Pi builtins; empty=none) + excludeTools. Concurrency: per-step default 4 (parallel-utils.ts:274), global semaphore default 20 overridable by globalConcurrencyLimit (parallel-utils.ts:146). Usage reporting for regular runs: totalTokens, totalCost, toolCount, turnCount, model/attemptedModels, durationMs, Fleet live token split. Watchdog spend NOT reported anywhere (review.ts:339-359 builds a standalone Agent over streamSimple with its own auth). Same for permission arbiter.

Skills: skills/pi-subagents/SKILL.md (delegation playbook: Direct vs Orchestrator; writer → challenge/simplify → review; launch shapes {agent,task}, workflowScript runs.run/all/lanes; refs prompting-and-roles, execution-controls, review-and-validation, multi-lane-orchestration, management-authoring-rpc, constraints-and-recipes). skills/council-mode/SKILL.md (2–4 read-only council-* advisors; pass 1 independent → pass 2 cross-exam → optional pass 3; parent writes memo).

Compaction: watchdog listens only to session_compact (hard reset of scope/activity tail; register-main.ts:438, runtime.ts:271-301). No session_before_compact handling anywhere in the package. Child-run abort recovery allows one compaction-abort recovery (runs/shared/abort-recovery.ts:92-114).

Reuse as-is: review loop for hosted Pi sessions (enable subagents.watchdog + WATCHDOG.md); finding contract (consume subagent_watchdog_warning); config shape/writer semantics (copy K-Dense watchdog-settings.ts pattern). Must extend: model/cost governance (unledgered reviews); child coverage for titan-spawned runners (pi -p children, Codex/Claude/external CLIs get nothing). Must build: cross-session / on-demand review, "review this runId" entrypoint, artifact-dir reviewer, session_before_compact handling — cheapest route is a thin extension reusing createMainWatchdogReview + MainWatchdogRuntime (internal, unexported, unversioned).
