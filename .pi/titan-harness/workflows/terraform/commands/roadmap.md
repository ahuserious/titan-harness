---
description: roadmap.md — the next four quarters as workstreams with sources, unknowns and the harness level each needs
---
$inputs.contract

# Task: roadmap.md

From the entity document and the sources, write a roadmap: three to seven workstreams,
each with a goal, the evidence that motivates it (cite `(src: <path>@<digest>)` or
`(entity: <section>)`), its dependencies, the risks, and the harness level it needs
(0 ultrafast · 1 brain+workers · 2 triggered ops · 3 engineering). Add a "Now / Next /
Later" table and a "Missing information" list of what a planner would still need.

Rules: only what the sources support; `unknown` where they are silent; no dates unless
a source states them; no execution claims.

## Sources (path · sha256 · bytes)
$inputs.sources

## Entity document
$entity.output
