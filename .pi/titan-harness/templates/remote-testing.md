# Remote testing — cloud simulated users

Advice written by `/cloud-simulated-users setup <provider>` (host-persisted; the setup agent is read-only).
Credentials appear by NAME only. Probe the lanes with `/cloud-simulated-users probe`.

## cursor-cloud — Cursor cloud agents

_vacant until CURSOR_API_KEY is exported and `.titan/cursor.yaml` names the environment._

## testmu-hyperexecute — TestMu AI cloud (HyperExecute)

_vacant until the `testmu` MCP server is enabled and logged in._

## kane-remote — KaneAI on cloud devices (kane-cli --remote)

_vacant until `@testmuai/kane-cli` is installed and `kane-cli login` ran._

## momentic — Momentic AI E2E

_vacant until MOMENTIC_API_KEY (or MOMENTIC_CONFIG) is exported and the `momentic` server is enabled._
